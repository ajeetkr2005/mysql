CREATE TABLE emp (
    empid INT PRIMARY KEY,
    empname VARCHAR(50),
    manager_id INT
);
INSERT INTO emp (empid, empname, manager_id)
VALUES
(1, 'Rishabh', NULL),
(2, 'Amit', 1),
(3, 'Rahul', 1),
(4, 'Priya', 2),
(5, 'Neha', 2);
SELECT * FROM emp;

SELECT 
    T2.empname Employee,
    T1.empname Manager
FROM emp T1
JOIN emp T2
ON T1.empid = T2.manager_id;
